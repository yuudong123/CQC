# CQC 모델 카드

> 실제 50개 실험 결과 확인과 최종 Test 1회 평가 후 확정한다. 미확정 값은 임의로 채우지 않는다.

최종 산출물은 `src/training/model_card.py`가 패키지·CV 비교·Test·신뢰도·CPU 측정 JSON을 읽어 생성한다.

## 식별 정보

- 모델명: TODO
- 모델 버전: TODO
- 체크포인트 SHA-256: TODO
- 모델 구조: `joint` 또는 `separate`
- 입력 장수: TODO
- 입력 크기: 224×224 RGB

## 목적과 출력

- 대상: 사과 부사·양광
- 품종 클래스 순서: `fuji`, `yanggwang`
- 품질 클래스 순서: `L`, `M`, `S`
- 품질 표시명: `L=특`, `M=상`, `S=보통`
- 출력: 품종·품질 확률, 예측값, 각 신뢰도, 추론시간, 모델명·버전

## 데이터와 평가

- 데이터셋: AI Hub 농산물 품질(QC) 이미지
- 그룹: 179개 사과, 25,024프레임
- 선택 근거: 5-fold CV Macro F1와 CPU 평균·최대·p95
- 5-fold 결과: TODO
- 최종 Test 결과: TODO
- 최종 Test 사용 횟수: 1회

## 운영 조건

- 목표 장치: Intel i7-4790 CPU
- 목표 처리시간: 500ms 이내
- 신뢰도 기준: 품종 TODO, 품질 TODO
- 낮은 신뢰도·시간 초과 정책은 백엔드가 담당한다.
- inference 서비스는 bin·DB 정책을 포함하지 않는다.

## 한계와 위험

- 품종 2종과 품질 3단계 외 입력은 보장하지 않는다.
- 촬영 환경, 품목, 품종 변화에 대한 일반화는 검증되지 않았다.
- CV fold별 품질 성능 변동과 실패 사례를 최종 결과에 기록한다.
- 중복 해시 이미지 5쌍은 원본에서 제거하지 않았다.
