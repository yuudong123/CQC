# 프로젝트 기획 및 조사

- 작성일: 2026-09-16
- 프로젝트명: **CQC**
- 현재 확보 품목: **사과**
- 사과 범위: **품종 2종**
- 확장 범위: **농산물 품목 최대 2종**. 두 번째 품목은 미정
- 데이터: [AI Hub 농산물 품질(QC) 이미지](https://aihub.or.kr/aihubdata/data/view.do?aihubDataSe=data&dataSetSn=149&topMenu=103)
- 프로젝트 기간: 1개월
- 문서 상태: 상세 기획안. MVP 운영 흐름, 역할, MySQL, CPU 배포 환경과 목표 처리량이 확정되었다. 품종명·품질 세부 라벨과 모델 구조는 데이터 분석 및 비교 실험 후 확정한다.

> 기획 인터뷰의 최신 확정 사항은 [의사결정 기록](./decision-log.md)을 기준으로 한다.

## 0. 프로젝트 한 문장 정의

현재 확보한 사과 이미지 중 **사과 품종 2종**을 대상으로 특·상·보통 품질 등급과 판정 신뢰도를 제시하고, 등급별 선별 목적지 또는 저신뢰 검수 대상으로 자동 분기하는 농산물 품질 검사 MVP다. 데이터와 일정이 허용하면 두 번째 농산물 품목을 추가하며, 전체 지원 범위는 최대 2개 품목으로 제한한다.

이 프로젝트의 핵심은 이미지 분류 모델만 만드는 데 있지 않다. 이미지 입력부터 등급 판정, 저신뢰 예외 처리, 선별 신호 생성, 결과 기록까지 이어지는 **품질 검사 업무 흐름**을 한 번에 시연하는 것이 목표다.

## 1. 프로젝트 효과 조사

### 1.1 해결하려는 문제

농산물 품질 판정은 크기, 형태, 외관 상태, 품종과 촬영 조건처럼 여러 시각 요소를 함께 고려해야 한다. 작업자가 많은 물량을 반복해서 검사하면 판정 속도와 일관성이 작업 환경과 숙련도에 영향을 받을 수 있다. 단순 색상 임계값이나 크기 기준만으로는 조명, 촬영각도, 배경, 개체 차이를 안정적으로 처리하기 어렵다.

한 달 프로젝트에서는 실제 유통 등급 판정을 완전히 대체한다고 주장하지 않는다. AI Hub가 제공하는 품질 등급 라벨을 기준으로 제한된 환경의 이미지 분류 모델을 만들고, 모델이 확신하지 못하는 입력을 사람이 확인하도록 분리한다. 이를 통해 자동 판정이 가능한 구간과 사람이 개입해야 하는 구간을 함께 보여준다.

### 1.2 As-Is / To-Be

| 구분 | As-Is | To-Be |
|---|---|---|
| 품질 확인 | 작업자가 이미지를 보고 등급을 반복 판정 | AI가 사과의 특·상·보통 등급과 신뢰도를 제시 |
| 판정 기준 | 작업자 경험과 개별 기준에 따라 차이가 생길 수 있음 | 동일 모델과 전처리 규칙으로 일관된 기준 적용 |
| 예외 처리 | 애매한 개체도 즉시 등급을 정해야 함 | 낮은 신뢰도 결과를 별도 검수 대상으로 분리 |
| 선별 작업 | 판정 후 사람이 분류 위치를 다시 결정 | 예측 등급에 따라 선별 목적지 신호를 자동 생성 |
| 결과 기록 | 판정 근거와 처리 결과가 흩어질 수 있음 | 입력, 예측 등급, 신뢰도, 처리 시각을 한 흐름으로 기록 |
| 모델 검증 | 전체 정확도만 보면 소수 등급 실패를 놓칠 수 있음 | 등급별 Precision·Recall·F1과 혼동행렬로 오류 확인 |

### 1.3 예상 사용자와 사용 장면

| 사용자 | 사용 장면 | MVP에서 제공할 기능 |
|---|---|---|
| 선별 작업자 | 촬영된 사과의 등급과 분류 위치 확인 | 이미지 입력, 등급·신뢰도 표시, 선별 목적지 안내 |
| 품질 관리자 | 등급별 오분류와 애매한 사례 검토 | 혼동행렬, 저신뢰 목록, 예측 이력 확인 |
| 시연 관람자 | 자동화 흐름 이해 | 이미지 입력부터 선별 신호까지 한 화면에서 확인 |

실제 생산라인 운영자, 농가와 유통업체를 대상으로 한 현장 요구사항 조사는 아직 수행하지 않았다. 위 사용자는 MVP 시나리오를 정의하기 위한 가정이다.

### 1.4 프로젝트 목표 KPI

| 영역 | 측정 지표 | 기초 목표 | 확정 시점 |
|---|---|---|---|
| 분류 성능 | 사과 3등급 Macro F1 | 기준선 결과 확인 후 확정 | 2주차 기준선 학습 후 |
| 등급별 성능 | 등급별 Recall과 F1 | 특정 등급의 실패가 가려지지 않도록 모두 공개 | 2주차 |
| 데이터 품질 | 손상 이미지, 라벨 누락, 중복 후보 수 | 전체 검사 결과 기록 | 1주차 |
| 일반화 | 촬영각도 또는 개체 그룹 분리 시험 | 무작위 분할과 비교 | 데이터 구조 확인 후 |
| 추론 | 사과 1개 다각도 묶음 처리 시간 | 모델 500ms 제한, CPU 실측 | 3주차 |
| 검수 | 저신뢰 결과의 수와 오분류 비율 | 임계값별 비교표 작성 | 3주차 |
| 자동화 | 입력부터 선별 신호 기록까지 성공률 | 정해진 시연 세트 반복 실행 | 4주차 |

목표 수치는 성과를 미리 주장하는 값이 아니다. 최종 발표에는 실제 시험 환경, 데이터 분할 방법과 측정 결과를 함께 기록한다.

## 2. AI 분석 데이터 조사

### 2.1 데이터 개요

AI Hub 공식 소개에서 확인한 내용은 다음과 같다.

- 분야: 농축수산
- 유형: 이미지
- 구축년도: 2020년
- 구축량: 정제·가공된 데이터 30만 건
- 원시 수집량: 10개 품목별 6만 건, 총 60만 건
- 대상 품목: 무, 배추, 양파, 마늘, 양배추, 감, 사과, 배, 감귤, 감자
- 품질 분류: 특, 상, 보통
- 이미지 형식: PNG
- 메타데이터 예시: 작물·품종, 촬영각도, 촬영장비·장소·날짜, 크기, 부피
- 라벨 항목 예시: 종 ID, 과일 종류, 품질 등급, 촬영면, 수직·수평 촬영각도, 높이, 너비, 무게
- 공개 기준모델: Inception V3 이미지 분류
- 공개 성능: F1-Score 0.89
- 다운로드: 소개 페이지에 다운로드 기능과 API 파일 목록이 제공되며, 데이터 신청과 승인 절차가 필요하다.

페이지 상단 소개에는 농산물 20종이라는 문구도 있으나, 상세 설명은 10개 품목을 열거한다. 프로젝트에서는 실제 다운로드 파일과 설명서를 확인하기 전까지 **10개 품목 상세 목록을 기준**으로 사용하고, 불일치 내용을 데이터 점검 기록에 남긴다.

### 2.2 MVP 품목 선정

#### 현재 범위: 사과 품종 2종

- 현재 확보된 이미지는 사과 데이터다.
- 사과 전체 품종을 한 범주로 합치지 않고, 팀이 선택할 **사과 품종 2종**을 데이터 범위로 둔다.
- 두 품종 모두 특·상·보통 품질 등급 데이터를 확보할 수 있는지 먼저 확인한다.
- 품종별 촬영 조건과 등급 분포 차이가 모델 판단에 미치는 영향을 따로 분석한다.
- 실제 파일과 라벨을 확인한 결과 품종은 **부사와 양광**으로 확정한다.

품종과 품질 등급은 모두 MVP의 필수 예측 대상이다. 단일 멀티태스크 모델과 품종·품질 분리 모델을 비교하고 품종 정확도, 품질 정확도, CPU 추론 속도와 운영 복잡도를 기준으로 최종 구조를 선택한다.

#### 두 번째 품목 확장 조건

다음 조건을 모두 만족할 때 두 번째 농산물 품목을 추가한다.

1. 사과 품종 2종의 데이터 로더와 학습 파이프라인이 재현 가능하게 동작한다.
2. 두 품종에 대한 3등급 기준선과 품종별 오류 분석이 완료된다.
3. 후보 품목의 등급별 표본 수와 라벨 구조를 확인한다.
4. 품목 추가가 4주차 통합과 발표 준비를 방해하지 않는다.

두 번째 품목은 아직 정하지 않는다. 전체 지원 범위는 사과를 포함해 최대 2개 품목이다.

### 2.3 입력과 예측 대상

| 구분 | MVP 정의 | 확인 상태 |
|---|---|---|
| 입력 | 동일 사과의 여러 각도 이미지 묶음 | 기획 확정, 사용할 각도·장수 TODO |
| 보조 정보 | 사과 품종, 촬영각도 등 메타데이터 | 품종별 분석에 사용, 모델 입력 여부 TODO |
| 정답 | 특·상·보통 품질 등급 | 공식 페이지 확인 |
| 출력 | 예측 품종·품질등급, 각 확률과 신뢰도 | 기획 확정 |
| 자동화 출력 | 품종 2종 × 품질 3단계의 6개 bin 또는 재검사 bin | 기획 확정 |
| 설명 정보 | 모델 주목 영역 시각화 | 구현 여유에 따른 확장 |

첫 기준선은 사과 품종 2종의 이미지를 입력으로 사용한다. 품종 라벨과 품질 라벨을 각각 예측하며, 실제 품종명과 품질 세부 근거는 확보 데이터의 라벨을 분석한 뒤 확정한다.

### 2.4 가장 중요한 데이터 누수 위험

공식 설명은 한 농산물을 여러 카메라 위치와 각도에서 반복 촬영한 구조를 보여준다. 같은 개체의 여러 이미지가 학습 데이터와 시험 데이터에 나뉘면 모델이 품질 일반화 대신 동일 개체의 특징을 기억하여 성능이 부풀 수 있다.

데이터 확보 후 다음 순서로 확인한다.

1. 파일명과 라벨에 개체 ID 또는 촬영 묶음 ID가 있는지 찾는다.
2. 동일 개체의 여러 각도 이미지가 몇 장씩 연결되는지 확인한다.
3. 개체 그룹을 식별할 수 있으면 `Group Split`을 사용한다.
4. 그룹 식별이 어렵다면 파일명 규칙, 촬영 시각과 이미지 유사도를 이용해 중복 후보를 조사한다.
5. 무작위 이미지 분할 성능과 그룹 분할 성능을 함께 기록한다.

이 검증이 끝나기 전에는 높은 시험 성능을 실제 새 농산물에 대한 일반화 성능으로 해석하지 않는다.

### 2.5 데이터 품질 점검

| 점검 대상 | 점검 내용 | 산출물 |
|---|---|---|
| 파일 | 손상, 확장자, 해상도, 읽기 실패 | 파일 검사 보고서 |
| 라벨 | 누락, 알 수 없는 등급, 이미지와 라벨 불일치 | 오류 목록 |
| 분포 | 품목·품종·등급·각도별 건수 | 분포표와 그래프 |
| 중복 | 동일 파일 해시, 유사 이미지, 동일 개체 촬영 | 중복 후보 목록 |
| 배경 | 등급과 배경·촬영장소의 우연한 연관 | 샘플 비교 및 오류 분석 |
| 분할 | 같은 개체가 여러 세트에 포함되는지 확인 | 분할 검증 결과 |

### 2.6 반드시 공개할 한계

- AI Hub 품질 등급은 제공 데이터의 기준이며, 모델 결과가 실제 유통 현장의 공식 등급 판정을 보증하지 않는다.
- 촬영 장비와 배경이 통제된 데이터라면 휴대전화나 현장 컨베이어 이미지에서 성능이 낮아질 수 있다.
- 품질 등급 차이를 만드는 세부 기준과 품종별 기준을 실제 라벨 설명서에서 추가 확인해야 한다.
- 표면에 보이지 않는 당도, 내부 손상과 맛을 RGB 이미지 한 장으로 직접 판단할 수 없다.
- 클래스별·품종별 실제 표본 수는 다운로드 전에는 확정할 수 없다.
- 물리 선별기를 연결하지 않으면 선별 자동화는 소프트웨어 신호와 화면 시뮬레이션 수준이다.
- Grad-CAM 같은 시각화는 모델이 본 영역을 설명하는 보조 자료이며 품질 판정의 인과적 근거가 아니다.

발표 표현은 **“AI Hub 이미지 라벨 기준 사과 품질 등급 판정과 선별 흐름 자동화 MVP”**로 통일한다.

### 2.7 분석 및 모델 설계

1. 데이터 설명서와 실제 파일 구조를 비교한다.
2. 사과 이미지만 추출하고 품질 등급 라벨을 정규화한다.
3. 품종, 등급, 촬영각도와 개체 그룹 후보의 분포를 분석한다.
4. 이미지 손상, 라벨 누락과 중복 후보를 제거하거나 별도 기록한다.
5. 가능한 경우 개체 단위로 학습·검증·시험 세트를 분리한다.
6. 사전학습 이미지 분류 모델로 기준선을 만든다.
7. 클래스 불균형이 확인되면 가중치 또는 샘플링을 비교한다.
8. 밝기, 회전, 크롭 등 증강은 실제 촬영 조건을 벗어나지 않는 범위로 제한한다.
9. 전체 정확도와 Macro F1, 등급별 Precision·Recall·F1, 혼동행렬을 기록한다.
10. 저신뢰 임계값별 자동 판정 비율과 오분류 비율을 비교한다.
11. 실제 시연 입력과 학습 데이터 간 차이를 오류 사례로 정리한다.

### 2.8 기준선 모델 후보

| 후보 | 역할 | 장점 | 주의점 |
|---|---|---|---|
| 단순 다수 클래스 예측 | 최소 기준 | 데이터 불균형 영향 확인 | 실제 이미지 학습 모델 아님 |
| 경량 CNN 전이학습 | 1차 기준선 | 한 달 프로젝트와 CPU/GPU 제약에 적합 | 모델 종류는 환경 확인 후 확정 |
| 조금 큰 사전학습 모델 | 비교 모델 | 기준선 대비 성능 개선 가능 | 학습 시간과 시연 PC 자원 확인 필요 |
| 이미지+메타데이터 모델 | 확장 실험 | 크기·품종 정보 활용 가능 | 실사용 입력에서 메타데이터 확보 필요 |

#### 다각도 그룹 모델 권장안

상용 사과 선별기는 사과를 개별 캐리어 또는 롤러에서 회전시키며 통제된 조명 아래 360° 외관을 다수 촬영한다. 공개된 상용 사례는 사과 한 개당 40장부터 최대 90장 또는 수백 장까지 생성한다. CQC도 동일 `group_no`를 하나의 사과 묶음으로 사용한다.

다만 상용 장비의 촬영 프레임 수를 i7-4790 CPU가 전부 CNN으로 처리한다고 가정하지 않는다. 촬영·입력 프레임 수와 실제 모델에 투입할 대표 프레임 수를 분리한다. 공유 특징 추출기와 결합 계층을 사용하고, 대표 시점 수에 따른 품종·품질 Macro F1과 CPU 지연을 비교하여 최종 입력 장수를 정한다.

현재 데이터의 179개 그룹 중 175개는 40장 이상이지만 4개 그룹은 8~30장만 제공한다. 또한 1000×1000 RGB 40장은 디코딩 시 사과당 약 114.4MB이므로 i7-4790에서 500ms 이내 처리를 보장할 수 없다. 40장은 현장 촬영 가정의 참고값으로 두고 모델 입력 장수는 실제 CPU 벤치마크로 축소할 수 있다.

구체적인 모델명과 프레임워크는 GPU, 팀 경험과 설치 환경을 확인하기 전까지 확정하지 않는다.

## 3. 서비스 및 자동화 설계

### 3.1 전체 흐름

```text
AI Hub 원본 이미지·라벨
  → 파일·라벨 검증
  → 사과 데이터 추출
  → 개체 그룹 확인 및 데이터 분할
  → 전처리·증강
  → 기준선 모델 학습·평가
  → 검증된 모델 저장

사용자 이미지 입력
  → 파일 형식·크기 검증
  → 학습과 동일한 전처리
  → 품질 등급 추론
  → 예측 등급·신뢰도 반환
  → 신뢰도 임계값 판정
      ├─ 기준 이상: 특·상·보통 선별 목적지 생성
      └─ 기준 미만: 재검사 bin으로 분기
  → 판정 이력과 처리 결과 기록
  → 웹 화면에 결과 표시
```

### 3.2 MVP 기능 범위

| ID | 기능 | 완료 기준 | 우선순위 |
|---|---|---|---|
| F-01 | 자동 이미지 입력 | 시뮬레이터가 시험 사과 그룹을 초당 2개씩 multipart로 전송 | 필수 |
| F-02 | 입력 검증 | 잘못된 형식과 읽기 실패를 사용자에게 알림 | 필수 |
| F-03 | 품종·품질 추론 | 사과 품종과 특·상·보통 품질 및 각 신뢰도 반환 | 필수 |
| F-04 | 저신뢰 분기 | 설정 임계값 미만을 검수 대상으로 표시 | 필수 |
| F-05 | 선별 신호 | 등급별 목적지 또는 검수 목적지를 생성 | 필수 |
| F-06 | 판정 이력 | 검사 시각, 품목·품종, 예측 등급, 신뢰도, 검수 여부와 선별 결과를 MySQL에 저장 | 필수 |
| F-07 | 검사 이력 조회 | 조건에 맞는 검사 이력을 웹에서 조회 | 필수 |
| F-08 | 통계·분석 조회 | 웹에서 기간·품목·품종·등급 기준의 검사 현황과 분포를 조회 | 필수 |
| F-09 | 주목 영역 표시 | 모델이 참고한 영역을 시각화 | 확장 |
| F-10 | 두 번째 품목 지원 | 확정된 후보 품목을 선택하여 해당 모델 사용 | 확장 |
| F-11 | 가상 제어 API | 검사 결과에 맞는 6개 정상 bin 또는 재검사 bin 명령 생성 | 필수 |

### 3.3 저신뢰 검수 정책

모델의 최대 클래스 확률이 임계값보다 낮으면 자동 선별하지 않고 검수 대상으로 분리한다. 임계값은 임의의 숫자로 확정하지 않는다. 검증 데이터에서 여러 임계값을 비교하여 다음 항목을 함께 본다.

- 자동 판정으로 처리되는 이미지 비율
- 자동 판정 구간의 정확도와 Macro F1
- 검수 대상으로 빠지는 이미지 수
- 특 또는 상을 보통으로 잘못 보내는 사례
- 보통을 특으로 잘못 보내는 사례

최종 임계값은 모델 성능과 시연 목적을 함께 고려해 정하고, 설정 파일에서 변경할 수 있게 한다.

### 3.4 선별 자동화 시뮬레이션

실제 설비와 PLC는 연동하지 않는다. 백엔드의 가상 제어 API 호출을 최종 자동화 범위로 하며, 화면 애니메이션은 MVP 이후로 미룬다.

| 판정 결과 | 선별 목적지 예시 | 화면 표시 |
|---|---|---|
| 특 | BIN_A | 특 등급 구역으로 이동 |
| 상 | BIN_B | 상 등급 구역으로 이동 |
| 보통 | BIN_C | 보통 등급 구역으로 이동 |
| 저신뢰 | REVIEW | 작업자 검수 대기 |

목적지 이름은 장비 규격이 아니라 임시 인터페이스다. 실제 장비를 연결할 경우 신호 형식, 통신 방식, 실패 시 안전 동작을 별도 요구사항으로 정의한다.

### 3.5 논리 아키텍처

| 영역 | 책임 | 입력 | 출력 |
|---|---|---|---|
| 데이터 파이프라인 | 원본 검사, 라벨 정규화, 분할, 전처리 | 원본 이미지·라벨 | 학습용 데이터와 분할 목록 |
| 학습 파이프라인 | 모델 학습, 평가, 모델 저장 | 학습·검증 데이터 | 모델 파일과 평가 결과 |
| 추론 모듈 | 이미지 전처리와 등급 예측 | 이미지 | 등급별 확률과 예측 등급 |
| 판정 정책 | 저신뢰 여부와 선별 목적지 결정 | 예측 결과 | 자동 선별 또는 검수 신호 |
| API 후보 | 화면과 추론 모듈 연결 | 이미지 요청 | 구조화된 예측 응답 |
| 웹 화면 후보 | 입력과 결과 표시 | 사용자 이미지 | 등급, 신뢰도, 처리 결과 |
| MySQL | 검사 이력 영구 저장, 통계·분석 조회 지원 | 판정 이벤트와 검사 결과 | 웹 조회용 이력·집계 데이터 |

데이터베이스는 **MySQL**을 사용한다. DB 목적은 검사 이력을 보존하고 웹에서 통계와 분석 결과를 조회할 수 있게 하는 것이다. 테이블, 컬럼, 인덱스, 관계와 마이그레이션 방식은 백엔드 담당자가 별도 문서로 설계한다. 이 기획서에서는 미확정 DB 구조를 임의로 정의하지 않는다. API와 웹 프레임워크는 팀 합의 전까지 확정하지 않는다.

### 3.6 데이터 계약 초안

#### 추론 요청

| 항목 | 설명 | 상태 |
|---|---|---|
| image | 판정할 이미지 파일 | 필수 |
| crop_type | 품목. MVP에서는 apple 고정 가능 | 설계 TODO |
| request_id | 요청 추적 ID | 선택 |

#### 추론 응답

```json
{
  "prediction_id": "TODO",
  "crop_type": "apple",
  "predicted_grade": "special|high|normal",
  "confidence": 0.0,
  "review_required": true,
  "sorting_target": "BIN_A|BIN_B|BIN_C|REVIEW",
  "model_version": "TODO"
}
```

영문 등급 코드와 화면에 보일 한국어 명칭의 매핑은 데이터 라벨을 확인한 뒤 확정한다.

## 4. 구현 기술 검토

### 4.1 기술 영역과 선택 기준

| 영역 | 필요한 기능 | 선택 기준 | 상태 |
|---|---|---|---|
| 언어 | 데이터 처리, 학습, 추론 | 팀 경험과 이미지 생태계 | Python 우선 검토 |
| 이미지 처리 | 로딩, 리사이즈, 증강 | 학습·추론 동일 처리 가능 | TODO |
| 모델 학습 | 전이학습과 평가 | 시연 장비, GPU 유무, 재현성 | TODO |
| API | 이미지 업로드와 추론 호출 | 단순성, 자동 문서화 | TODO |
| 웹 | 입력, 결과, 이력 표시 | 1개월 구현 난이도 | TODO |
| 데이터베이스 | 검사 이력 저장, 웹 통계·분석 조회 | 이력 조회와 집계 요구, 백업·재현성 | **MySQL 확정**, 상세 구조는 백엔드 담당 문서 TODO |
| 테스트 | 데이터·추론·API 검증 | 핵심 실패를 재현할 수 있어야 함 | TODO |
| 실행 | 팀 PC에서 동일 실행 | 설치 재현성과 시연 안정성 | TODO |

### 4.2 기술 스택 확정 원칙

1. 팀원이 한 달 안에 구현하고 설명할 수 있는 기술을 선택한다.
2. 핵심 시나리오에 필요하지 않은 분산 시스템과 복잡한 MLOps 도구는 넣지 않는다.
3. GPU가 없는 환경에서도 저장된 모델로 추론할 수 있어야 한다.
4. 학습 코드와 서비스 추론이 동일한 전처리 정의를 사용한다.
5. 모델 파일, 원본 데이터와 생성 결과는 Git 저장소에 직접 올리지 않는다.

### 4.3 Git 운영 초안

- `main`: 발표 전까지 일반 통합에 사용하지 않고 최종 발표 버전을 표시한다.
- `dev`: 통합과 학원 PC 자동 배포 기준 브랜치다.
- `feat/*`: 개인 기능 작업 브랜치다.
- `feat/* → dev`는 Pull Request와 최소 1명 확인 후 병합한다.
- `main`에는 자동 배포를 연결하지 않는다.

- 안정 브랜치명은 저장소 생성 후 결정한다.
- 작업 브랜치 후보: `feat/data`, `feat/model`, `feat/inference`, `feat/api`, `feat/web`, `docs/plan`
- 한 사람이 동시에 같은 파일을 크게 수정하지 않도록 문서와 코드 소유 영역을 합의한다.
- API 요청·응답과 데이터 필드는 구현 전에 문서에서 먼저 합의한다.
- 원본 데이터, 모델 가중치와 대용량 출력은 `.gitignore`로 제외한다.
- 통합 전에는 최소 한 명이 변경 내용과 실행 결과를 확인한다.

## 5. 한 달 실행 계획

### 5.1 주차별 목표

| 주차 | 핵심 목표 | 주요 작업 | 완료 기준 |
|---|---|---|---|
| 1주차 | 계약·데이터 기반 | API·상태 계약, ZIP·JSON 분석, group_no 분할, mock 화면·API, Compose 골격 | 네 파트가 고정 계약과 mock으로 독립 실행 |
| 2주차 | 핵심 기능 구현 | 다각도 로더·기준선, 검사 오케스트레이션, DB·관리 화면, dev CI | 그룹 모델·상태 전이·이력 흐름이 독립 시험 통과 |
| 3주차 | 모델 선정·통합 | 4·8·12·16·40장 및 단일·분리 모델 비교, 실제 inference와 전체 HTTP 흐름 연결 | simulator부터 웹·DB·가상 제어까지 연결 |
| 4주차 | 성능·장애·발표 | i7-4790 성능, 장애 5종, 보존 정책, 재시작, CSV, 반응형 화면과 발표 검증 | 10-13 기능 동결 후 동일 버전으로 반복 시연 |

### 5.2 역할 구성

프로젝트는 다음 네 영역으로 나눈다. 역할별 담당자는 다음과 같이 확정한다.

| 역할 | 담당자 | 핵심 책임 | 주요 산출물 | 다른 역할에 제공하는 것 |
|---|---|---|---|---|
| 데이터 분석·모델 담당 | 조현재 | group_no 분석·분할, 다각도 그룹 로더, 단일·분리 모델과 입력 장수 비교, 신뢰도·CPU 평가, inference API | 매니페스트, 분할 목록, 학습 코드, 모델 카드·파일, inference 계약 | 백엔드가 호출할 품종·품질 예측 서비스와 MLOps 성능 기준 |
| 프론트엔드 담당 | 강성민 | 무스크롤 대시보드, 처리 카드, 이력·통계·CSV, 장애 이미지·오판 의심·시연 토글 화면 | 반응형 화면, API 연동, 상태·오류 UI, 프론트 기술 문서 | 백엔드 API를 사용하는 운영·관리·시연 화면 |
| 백엔드 담당 | 홍준희 | multipart 검사 요청, inference 조정, 상태·재검사·제어 정책, MySQL·통계·CSV·이미지 생명주기 | OpenAPI, 판정 정책, DB 설계·마이그레이션, simulator·가상 제어 | 프론트 조회 API와 MLOps가 배포할 backend·simulator 서비스 |
| MLOps·CI/CD 담당 | 홍유나 | 5개 Compose 서비스, healthcheck, dev CI/CD, 모델·볼륨·로그·위치 복구, CPU 시험 환경 | Compose·CI, 배포·복구 문서, 자동 수용시험, 모델 버전 규칙 | 네 영역을 i7-4790 환경에서 검증·실행하는 통합 기반 |

#### 역할 경계

- 데이터 분석·모델 담당은 `다각도 이미지 묶음 → 품종·품질 확률`까지 책임진다.
- 백엔드 담당은 모델 출력을 받아 정상 6개 bin 또는 재검사 bin을 결정하고 MySQL 이력과 내부 가상 제어를 책임진다.
- 프론트엔드 담당은 자동 검사 상태와 백엔드 조회·관리 기능을 화면으로 표현한다. 사용자 이미지 업로드는 제공하지 않는다.
- MLOps·CI/CD 담당은 모델과 서비스가 정해진 시험을 통과해야 배포되도록 구성한다.
- 모델 전처리는 학습 코드와 백엔드 추론에서 중복 구현하지 않고 데이터·모델 영역의 공통 모듈을 사용한다.
- API 요청·응답과 모델 입력·출력 형식은 구현 전에 네 담당자가 함께 확정한다.

### 5.3 논리적 마일스톤과 의존성

마일스톤은 날짜보다 산출물의 선후 관계를 기준으로 한다. 각 단계는 선행 마일스톤의 완료 기준을 충족해야 다음 통합 단계로 넘어간다.

| ID | 마일스톤 | 선행 의존성 | 데이터 분석·모델 | 프론트엔드 | 백엔드 | MLOps·CI/CD | 공통 완료 기준 |
|---|---|---|---|---|---|---|---|
| M1 | 요구사항·계약 동결 | 없음 | 로컬 라벨·group_no·분할 규칙 확정 | 화면 상태·mock 응답 정의 | multipart·inference·상태·오류·DB 논리 계약 | 브랜치·Compose·CI 원칙 정의 | 네 역할의 계약과 소유 문서 합의 |
| M2 | 독립 개발 기반 구축 | M1 | 그룹 EDA·70/15/15·5-Fold·다각도 로더 | mock 기반 반응형 대시보드·관리 화면 | mock inference API·MySQL 스키마·마이그레이션 | 5개 Compose 서비스와 feat PR·dev CI | 네 영역이 mock으로 독립 실행 |
| M3 | 핵심 기능 구현 | M2 | 기준선·그룹 결합 모델과 지표 | 처리 카드·이력·통계·CSV UI | 상태 전이·500ms·DB·내부 가상 제어·이미지 정책 | healthcheck·모델·볼륨·배포 기반 | 핵심 기능이 영역별 시험 통과 |
| M4 | 모델 선정·전체 흐름 연결 | M3 | 4·8·12·16·40장과 단일·분리 모델 비교, inference API | 실제 API·1초 갱신 연동 | simulator·inference·DB·제어·프론트 연결 | i7-4790 시험 환경·통합 배포 | simulator부터 웹까지 전체 흐름 작동 |
| M5 | 성능·장애·운영 검증 | M4 | 신뢰도·병렬 수 결정 | 무스크롤·상태·장애 5종 화면 검증 | 순환 삭제·DB 장애·지연 결과·CSV 검증 | 재시작·로그·기존 버전 유지·수용시험 | 초당 2개와 정상·장애 시나리오 반복 성공 |
| M6 | 기능 동결·발표 준비 | M5 | 최종 Test 1회·모델 카드 | 10-13 화면 동결·발표 흐름 | OpenAPI·DB 계약 동결 | 배포·복구 절차와 발표 버전 동결 | 문서·발표·실행 버전 일치 |

### 5.4 핵심 의존성

```text
M1 요구사항·계약
  └─ M2 독립 개발 기반
       └─ M3 핵심 기능 구현
            └─ M4 모델 선정·전체 흐름 연결
                 └─ M5 성능·장애·운영 검증
                      └─ M6 기능 동결·발표 준비

데이터·모델: 데이터 분할 → 기준선 학습 → 최종 모델·추론 규격
                                      ↓
백엔드: 임시 모델 API → 실제 모델 연결 → 판정 정책·이력
                                      ↓
프론트엔드: 임시 응답 UI → 실제 API 연결 → 시연 화면

MLOps·CI/CD: 기본 실행 환경 → 영역별 자동 검사 → 통합 배포·버전 동결
```

#### 병렬 진행이 가능한 작업

- M1에서 계약을 합의한 뒤 M2의 네 역할 작업은 동시에 진행할 수 있다.
- 프론트엔드는 백엔드 완성을 기다리지 않고 M1의 예시 응답으로 화면을 개발한다.
- 백엔드는 최종 모델을 기다리지 않고 동일한 함수 규격을 가진 임시 모델로 API를 개발한다.
- MLOps·CI/CD는 각 영역의 최소 실행 명령이 생기는 즉시 자동 검사를 붙인다.

#### 통합을 막는 의존성

- 데이터 분할이 확정되지 않으면 최종 모델 성능을 승인할 수 없다.
- 모델 입력·출력 규격이 확정되지 않으면 백엔드의 실제 모델 연결을 완료할 수 없다.
- API 계약이 확정되지 않으면 프론트엔드와 백엔드의 통합 완료를 선언할 수 없다.
- 영역별 실행 명령과 테스트 기준이 없으면 MLOps·CI/CD가 통합 배포를 완료할 수 없다.
- 두 번째 농산물 품목은 이번 필수 WBS에서 제외하고 사과 MVP 완료 후 별도로 평가한다.

### 5.5 역할별 인수인계 계약

| 제공자 | 받는 역할 | 인수인계 항목 | 인수 조건 |
|---|---|---|---|
| 데이터 분석·모델 | 백엔드 | 모델 파일, 전처리 함수, 클래스 순서, 입력 크기, 예측 응답 예시 | 고정 시험 이미지에서 같은 결과 재현 |
| 데이터 분석·모델 | MLOps·CI/CD | 모델 버전, 평가 지표, 승인 기준, 시험 데이터 목록 | 자동 검사에서 모델 로딩과 최소 성능 기준 확인 가능 |
| 백엔드 | 프론트엔드 | 추론·이력·통계 API 명세와 정상·오류·저신뢰 응답 예시 | 예시 요청으로 검사·이력·통계 화면 상태 재현 가능 |
| 백엔드 | MLOps·CI/CD | 실행 명령, 환경 변수, 상태 확인 경로, MySQL 마이그레이션·초기화 방법 | 새 환경에서 MySQL과 백엔드를 시작하고 상태 확인 가능 |
| 프론트엔드 | MLOps·CI/CD | 빌드·실행 명령, 백엔드 주소 설정, 정적 산출물 | 통합 환경에서 백엔드 주소를 주입해 실행 가능 |
| MLOps·CI/CD | 전체 | 공통 실행법, 자동 검사 결과, 배포 버전과 로그 위치 | 팀원이 같은 절차로 시연 환경 재현 가능 |

역할별 담당자는 데이터 분석·모델 조현재, 프론트엔드 강성민, 백엔드 홍준희, MLOps·CI/CD 홍유나로 확정한다.

## 6. 검증 계획

### 6.1 모델 검증

- 학습·검증·시험 데이터의 등급별 건수를 공개한다.
- Accuracy만 사용하지 않고 Macro F1과 등급별 Precision·Recall·F1을 함께 본다.
- 혼동행렬로 어떤 등급 사이에서 오류가 발생하는지 확인한다.
- 동일 개체의 여러 각도 이미지가 세트 사이에 섞이지 않았는지 검사한다.
- 가장 성능이 좋은 결과뿐 아니라 사용한 seed, 분할과 설정을 기록한다.
- 틀린 예측과 낮은 신뢰도 예측의 공통 조건을 이미지로 검토한다.

### 6.2 서비스 검증

| 시험 | 기대 결과 |
|---|---|
| 정상 사과 이미지 입력 | 등급, 신뢰도, 선별 목적지 표시 |
| 지원하지 않는 파일 입력 | 명확한 오류 메시지 반환 |
| 손상 이미지 입력 | 추론하지 않고 입력 오류로 처리 |
| 저신뢰 이미지 입력 | 자동 선별 대신 REVIEW로 분기 |
| 연속 이미지 처리 | 각 요청에 고유 판정 ID와 이력 생성 |
| 모델 파일 누락 | 서비스가 원인을 알 수 있는 오류 반환 |
| MySQL 연결 실패 | 저장 실패를 숨기지 않고 DB 오류로 처리 |
| 이력 조건 조회 | 조건과 일치하는 검사 기록만 반환 |
| 통계·분석 조회 | 기간·품목·품종·등급 기준 집계가 이력과 일치 |

### 6.3 최종 시연 시나리오

1. 서로 다른 품질 등급의 사과 이미지를 차례로 입력한다.
2. 화면에서 예측 등급과 신뢰도를 확인한다.
3. 시스템이 BIN_A, BIN_B, BIN_C 중 하나를 선택하는 모습을 보여준다.
4. 애매한 이미지를 입력해 REVIEW로 분리되는 모습을 보여준다.
5. 최근 판정 이력에서 입력과 처리 결과를 확인한다.
6. 기간·품종·등급별 통계와 저신뢰 비율을 확인한다.
7. 시험 데이터의 혼동행렬과 대표 오류 사례를 설명한다.

## 7. 주요 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 데이터 승인 또는 다운로드 지연 | 학습 일정 지연 | 첫 주 최우선 작업으로 처리하고 샘플 제공 여부 확인 |
| 사과 품종별 실제 건수 부족 | 품종별 모델 성능과 검증 신뢰도 저하 | 두 품종의 등급별 분포를 확인하고 두 번째 품목 추가보다 사과 범위를 우선 조정 |
| 동일 개체 이미지 누수 | 시험 성능 과대평가 | 그룹 분할과 중복 후보 검사 수행 |
| 촬영 배경 편향 | 현장 이미지 일반화 실패 | 배경별 오류 분석과 제한 사항 공개 |
| 클래스 불균형 | 소수 등급 성능 저하 | Macro F1 사용, 가중치와 샘플링 비교 |
| 기술 스택 과다 | 통합 실패 | 필수 흐름 중심으로 선택하고 확장 기능 후순위 배치 |
| 실제 장비 없음 | 실제 선별 동작 검증 불가 | 초당 사과 그룹 2개 입력 시뮬레이터와 가상 제어 API로 흐름 검증 |
| 외부 이미지 성능 저하 | 현장 적용 주장 어려움 | AI Hub 조건 내 결과와 외부 시험 결과를 구분하여 보고 |

## 8. MVP에서 제외하는 범위

- 설비 고장예지와 설비보전
- 농산물 3종 이상의 동시 지원
- 내부 품질, 당도와 맛의 판정
- 실제 유통 등급 인증 또는 가격 자동 결정
- 대규모 실시간 스트리밍과 분산 처리
- 복잡한 자동 재학습과 무중단 배포
- 장비가 확정되지 않은 상태의 컨베이어 제어
- 모바일 앱과 관리자 시스템 전체 구축

## 9. 의사결정 대기 목록

| 항목 | 현재 상태 | 결정에 필요한 정보 |
|---|---|---|
| 팀 인원과 역할 | 확정 | 조현재: 데이터·모델, 강성민: 프론트엔드, 홍준희: 백엔드, 홍유나: MLOps·CI/CD |
| 정확한 일정 | TODO | 시작일, 중간 점검일, 발표일 |
| 사과 품종 2종 | 방향 확정, 품종명 TODO | 실제 사과 품종·등급별 데이터 분포 |
| 두 번째 품목 | 확장 후보, 품목명 미정 | 사과 품종 2종의 MVP 진행률과 후보 품목 데이터 구조 |
| 모델 프레임워크 | TODO | GPU, 설치 환경, 팀 경험 |
| API와 웹 기술 | TODO | 팀 경험과 시연 환경 |
| 데이터베이스 | MySQL 확정 | 백엔드 담당자가 DB 구조와 마이그레이션을 별도 문서화 |
| 저신뢰 임계값 | TODO | 검증 데이터 비교 결과 |
| bin 코드·매핑 구조 | TODO | 백엔드 DB 담당자 설계 |
| 모델 성능 승인값 | TODO | 기준선 결과와 클래스 분포 |

## 10. 프로젝트 실행 문서

이 문서는 문제, 데이터 적합성, 분석 설계, 서비스 범위와 실행 원칙을 다룬다. 세부 항목은 다음 문서에서 이어서 관리한다.

- 기능과 수용 기준: [`(서식)프로젝트 요구사항.md`]((서식)프로젝트%20요구사항.md)
- 학습 데이터와 판정 이력 필드: [`(서식)테이블(데이터) 명세서.md`]((서식)테이블(데이터)%20명세서.md)
- 구성 요소와 연결: [`(서식)아키텍처.md`]((서식)아키텍처.md)
- 일정과 담당: [`(서식)WBS.md`]((서식)WBS.md)

문서 반영 순서는 **프로젝트 요구사항 → 데이터/테이블 명세 → 아키텍처 → WBS**로 유지한다. 앞 문서에서 확정된 내용을 뒤 문서가 참조하도록 하여 서로 다른 정의가 생기지 않게 한다.
