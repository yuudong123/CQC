FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

COPY requirements-inference.txt ./
RUN pip install --no-cache-dir -r requirements-inference.txt

COPY src ./src

EXPOSE 8001

CMD ["python", "-m", "src.inference.api", "--model-dir", "/app/models/approved", "--device", "cpu", "--host", "0.0.0.0", "--port", "8001"]
